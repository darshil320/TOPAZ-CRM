# packages/shared — Shared Contracts (planned)

Typed contracts shared across `apps/*`: visit event payloads, consent records, webhook schemas, enquiry/lead shapes. Changed in a dedicated PR before any app consumes them (master plan §10.3).
