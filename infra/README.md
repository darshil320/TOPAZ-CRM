# infra — Deployment & Provisioning (planned)

Docker compose (dev), Railway/Render (backend), Vercel (dashboard), Jetson provisioning script (JetPack/TensorRT/watchdog/outbound-only HTTPS). CI: lint + typecheck + unit + integration (testcontainers: postgres+pgvector, redis) on every PR. See master plan §2.
