# Phase 2 build state

Update at END of every session. This file is the cross-session memory.

## Module status

| # | Module | Status | Session notes |
|---|---|---|---|
| 01 | foundation (migrations 0011-0019, doc_series, GST engine) | done | gates green (111 pytest incl. 24 GST goldens, numbering concurrency, payments immutability, outstanding view); not pushed to prod |
| 02 | quotations-api | done | BACKEND (prior session): quotation_repo + /api/quotations router + deps + 4 empirical tests green. UI (this session): quotes list + QuoteBuilder (create/edit) + detail (items/totals/revision chain) + actions.ts (create/update/revise/delete → FastAPI, 10s timeout, API-Key). Client GST mirror `lib/gst.ts` matches gst.py goldens (3500→140/140/3780 intra; 280 inter; 350-disc→126/126/3402). types.ts extended (products/quotations/quotation_items). Nav item added. tsc clean. |
| 03 | quote-pdf-send-approval | verified | CODE done + verified: pdf.py (Playwright, lazy), quote_html.py + templates/quotation.html (Jinja, amber brand, amount-in-words), storage.py (private bucket + signed URL), tasks/pdf.render_quotation_pdf, tasks/quotes.send_quotation + notify_quote_decision (24h-window branch, WA_MEDIA_ENABLED flag), send_wa_document, api/public.py (token-gated GET/approve/reject, idempotent, IP-stamped, pipeline upsert), /quotations/{id}/send. Dashboard: /q/[token] public page + ApproveActions, Send button, middleware excludes /q. Tests: test_num_words + test_quote_html (11 pure) + test_quotations_send_empirical (4, green on pgtest). tsc clean. **2026-07-26 SMOKE PASS on prod**: real quote → PDF rendered → WhatsApp received on handset (in-window text path, link + PDF). Required a prod fix (see decisions 07-26): sync Playwright crashed inside asyncio.run — fixed via asyncio.to_thread (commit 0a43348). Meta templates created (payment_due Active; quote_sent + quote_approved_confirm still In review — only gate the OUT-of-window path). |
| 04 | orders-pipeline | in-progress | CODE done + verified: order_repo (create_from_quote approved-only + copies totals verbatim + advance=DEFAULT_ADVANCE_PCT; create_order manual; set_status optimistic + audit reason; patch_order), services/order_status.py (pure transition map), api/orders.py (from-quote/manual/PATCH status guarded 409/PATCH order). Dashboard: orders list (+order_outstanding), order detail (items/totals/outstanding/schedule/payments/timeline) + OrderStatusActions, pipeline kanban (/dashboard/pipeline, HTML5 drag → moveCustomerStage), "Create order" on approved quote. types.ts extended (orders/order_items/payments/payment_schedules/order_outstanding view); nav Orders+Board. Tests: test_order_status (4 pure) + test_orders_empirical (3, green on pgtest). tsc clean. |
| 05 | payments | in-progress | CODE done + verified: payment_repo (INSERT-only; order_totals nets refunds; mark_earliest_schedule_paid; replace_schedule; due_schedules pending→due). api/payments.py POST (guards: amount>0, refund=owner/admin only 403, over-payment 409 unless override+elevated, role resolved server-side from recorded_by, RCP numbering, schedule flip, receipt enqueue). orders POST /{id}/schedule. receipt_html + templates/receipt.html + tasks/receipts.render_receipt (Storage + documents + optional customer WA gated by SEND_RECEIPTS_TO_CUSTOMER). tasks/payment_reminders daily 10:00 IST → payment_due (utility template bypasses marketing-consent via FollowupTemplate.category + _skip_reason). Dashboard: RecordPaymentForm on order detail, accounts /dashboard/payments (collections + aging 0-7/8-30/30+). Tests: test_payments_empirical (4: immutability UPDATE/DELETE, refund netting, schedule flip, due reminders) green; FULL suite 141 passed. tsc clean. NOTE: security-reviewer pass on payments router still pending (M07). |
| 06 | roles-rls-admin | in-progress | CODE done + verified: migration 0020_rls_phase2a (app_settings table + owner/admin-write RLS + seeded defaults; note 0018/0019 taken by pipeline so RLS lands at 0020, not exec-plan's 0018). Quote/order/payment RLS already shipped in 0014-0016; test_rls_phase2a.py PROVES the matrix (7 assertions: unrelated-sp blind to quote, assigned-sp + accounts + owner read, sp cannot insert payment, accounts can, payment immutable even for owner, workshop blind to payments/quotes). Dashboard: /owner/admin (products CRUD + settings backed by app_settings + WhatsApp template registry read-only); admin nav; new-quote builder now defaults terms+validity from app_settings. Full suite 148 passed; tsc clean. PARTIAL: role-based login routing for accounts/workshop/delivery landing NOT wired (accounts reach /dashboard/payments via nav; owner /owner/admin gated). security-reviewer pass deferred to M07. |
| 07 | 2a-hardening (E2E, seed, UAT prep, reviews) | in-progress | seed_demo.py (idempotent, additive; payments immutable so re-seed skips) + test_seed_demo_empirical green. Playwright config + money-path E2E spec (MANUAL — needs live stack; skips without creds). code-reviewer + security-reviewer run on the 2A surface. FIXED: sec CRITICAL-1 (payments API role-gated accounts/owner/admin — RLS bypassed on service path), CRITICAL-2 (over-payment TOCTOU → SELECT FOR UPDATE), code HIGH (quote-send retry-dup guard via status-flip-first; receipt WA wrapped; payment enqueue failure no longer 500s), MEDIUM (XFF last-hop for approved_ip; payments page deterministic order), LOW (quote_html half-up). OPEN go-live BLOCKERS: sec HIGH-3 (caller identity trusted from body — needs JWT forwarding) + HIGH-4 (order/quote writes skip assignment check server-side). Docs: UAT_2A.md, TRAINING_2A.md, GO_LIVE_2A.md. Full pytest 149 passed; tsc clean. NOT deployed (staging project still not created; prod push at go-live). |
| 08 | workshops-media (migrations 0023-0025) | in-progress | BACKEND DONE + verified. Migrations renumbered 0014-0016/0019-0021 → **0023_workshops / 0024_production / 0025_media** (both planned ranges were taken; HEAD was 0022). Storage bucket policy deliberately OUTSIDE migrations (`supabase/storage/0025_media_policies.sql`) — pgtest's bare cluster has no `storage` schema. Shipped: `is_workshop_manager_of()`, 11 seeded stage defs (photo_required on frame_work/finishing/quality_inspection/dispatch), `production_event_apply()` denorm trigger, `sync_order_item_workshop()`, append-only `production_events`, polymorphic `media`, `services/media_entities.py` (pure), workshop/media/production/audit repos, 3 routers (8 routes), `tasks/media.py::make_thumb` (Pillow lazy), realtime publication folded in (module 11 needs no migration). Tests: 14 empirical + 33 pure; **full pgtest 237 passed**; pure suite green with Pillow NOT installed. UI shipped (in a parallel session, now committed): `/dashboard/production/allocate` + AssignModal + actions, `owner/admin` WorkshopAdmin tab, `components/media/MediaUpload`+`MediaGallery`, `lib/media/actions.ts`. `tsc --noEmit` clean. **STILL OPEN: code-reviewer + security-reviewer never ran on the 2B surface — both agents died on an API session limit.** Until those close, 08 has not formally met its acceptance gate. |
| 09 | production-engine | todo | |
| 10 | workshop-pwa | todo | |
| 11 | production-board | todo | |
| 12 | notifications-watchdog | todo | |
| 13 | 2b-hardening | todo | |

Status values: todo / in-progress / done / verified (gates green + user demo passed).

## Decisions log
<!-- append: date · module · decision · why -->
- 2026-07-04 · setup · Plan docs created from execution-plan report; conventions pinned in PLAN.md.
- 2026-07-22 · setup · Migrations RENUMBERED 0007→0011… (Phase 1 M5/M6B took 0007-0010). Ledger + specs to reflect. EXECUTION_PLAN_2A_2B.md §0.1.
- 2026-07-22 · setup · Built Docker-free empirical harness `apps/api/scripts/pgtest.sh` (temp PG15 cluster + pgvector + anon/authenticated/service_role + auth.uid() stub + all migrations). Runs RLS + repo suites without `supabase start`. PG15 chosen for prod parity (security_invoker views).
- 2026-07-22 · 01 · pipeline_stage strategy = FULL REMAP (user decision). Added 8 new stages (0018), data-migrated old→new (0019: new→inquiry, talking→design_discussion, follow_up→negotiation, won→order_confirmed, lost kept). Old enum values retained (PG can't drop) but unused. Updated 8 shipped dashboard files to new vocabulary; est-value/win analytics re-pointed won→order_confirmed. tsc clean.
- 2026-07-22 · 01 · GST rounding: per-line pre-tax rounded 2dp then summed; document discount pro-rated at full precision by line share; CGST/SGST/IGST accumulated full-precision, rounded half-up 2dp at document level. 24 golden cases pin exact values.
- 2026-07-22 · 01 · Payments hard-immutable via `forbid_payment_mutation()` trigger (blocks UPDATE/DELETE incl. service role) + insert-only grant. Corrections = refund rows only.
- 2026-07-22 · 01 · GST inclusive/exclusive + HSN-per-family still UNANSWERED by client → built on documented fallback (exclusive, 18%, 9403). Goldens must be re-confirmed with Hemant before 2A go-live (module 07).
- 2026-07-24 · 02 · Module 02 UI shipped (quotes list/builder/detail + actions.ts); client GST mirror lib/gst.ts matches gst.py goldens; committed 46e0129.
- 2026-07-24 · 07/security · Closed sec-review HIGH-3/HIGH-4: write routes (quotations/orders/payments) now derive caller identity from a VERIFIED Supabase JWT (deps.get_caller_uid, HS256 via SUPABASE_JWT_SECRET, fail-closed 503) resolved to salesperson+role (api/authz.resolve_caller), and enforce the write matrix server-side (assert_can_write_customer: owner/admin any, salesperson only assigned, others 403). RLS is bypassed on the service-role connection so this API layer IS the authz boundary. Dashboard forwards the access token (lib/apiAuth.ts). recorded_by/created_by/salesperson_id no longer trusted from the body. Proven by test_authz_empirical. Remaining sec: LOW rate-limiting + a re-review of the JWT change (go-live).
- 2026-07-24 · scope · SCOPE FLAG RAISED (docs/phase2/PLAN_2A_SELL.md §0): every SOW on file (topaz-sow-v1 §4, superseded crm-sow-3L §3) EXCLUDES orders + payment tracking, reserving them for a "separate Phase 2 SOW"; the current signed CRM SOW (crm-sow-1.5L) does not name quote/order/payment. VENDOR DECISION (Darshil, DMC): proceed to build M03–M07 (quote send/approval, orders, payments, roles/RLS, hardening) now; vendor owns papering the Phase-2 SOW/CR before invoicing. Flag recorded per CLAUDE.md — not silently dropped. Feature-flag customer WA delivery until WA-MEDIA-SPIKE + Meta verification clear.
- 2026-07-25 · 07/K1 · WA-MEDIA-SPIKE **PASS**: real PDF + image sent to +91-9426529230 via the live Cloud API (phone_id 1189429440922862) with `scripts/wa_media_spike.sh` inside an open 24h window; Meta accepted both (wamids issued, no 131047/131026) and the **PDF was received on the handset** (delivery confirmed). Media send end-to-end works. (statuses-webhook line optional to eyeball in Railway logs; PDF arrival already proves delivery.)
- 2026-07-25 · 07/K2 · Customer media delivery **ENABLED**: `WA_MEDIA_ENABLED=true` set on Railway prod (user), Meta business verification reported complete. Chromium/PDF engine **confirmed live** in the deployed image — prod runs commit `2dafcae` whose `apps/api/Dockerfile:19` carries `RUN playwright install --with-deps chromium` (f579cb8 ancestor), build SUCCESS 19:28 IST. Prod `documents` + `face-crops` Storage buckets both private (public=false). STILL PENDING before first real customer send: confirm the 2A Meta **templates approved** (`quote_sent`, `quote_approved_confirm`, `payment_due`) — business verification ≠ template approval; and render one real quote PDF on prod (>10KB) as the final smoke.
- 2026-07-25 · 07/K3-K4 · 2A Meta templates **created** in WhatsApp Manager (WABA 1506116721198796), all Category=Utility, English, NAMED params matching code byte-for-byte: `quote_sent` (customer_name, quote_no, link), `quote_approved_confirm` (customer_name, quote_no), `payment_due` (customer_name, amount, order_no, due_date). `payment_due` **Active**; other two **In review**. No K4 code change needed — names/params already hardcoded (quotes.py inline sends) or registered (services/templates.py `payment_due`). Pinned as external contract in quotes.py module docstring. NOTE: doc typo `quote_number` in the pasted Track-K checklist is not in any repo file — code uses `quote_no`.
- 2026-07-26 · 03 · **PDF render crash fixed (commit 0a43348).** `render_html_to_pdf` uses Playwright's SYNC API but `_render_and_store` (quotes) and `_render` (receipts) both run under `asyncio.run()`, where sync Playwright raises "Sync API inside the asyncio loop". Every quote/receipt PDF render crashed → `send_quotation` retried → `mark_sent` guard short-circuited → quote flipped 'sent' with NO pdf + NO WhatsApp delivered. Fix: `await asyncio.to_thread(render_html_to_pdf, html)` in both tasks (fresh thread has no loop). "Chromium confirmed live" in the 07-25 K2 note only checked the binary, never a real render — this was the first real render.
- 2026-07-26 · 03 · **First real end-to-end quote send PASSED on prod** to Rajesh Mehta (customer 05b883dc, wa_id 919426529230): PDF rendered + WhatsApp received on handset via in-window text path (link + PDF). notify/approve public page reachable. Out-of-window template path still unproven (quote_sent/quote_approved_confirm In review at Meta).
- 2026-07-26 · 03 · **KNOWN GAP — dashboard conversation is not live.** `messages` table was never added to the `supabase_realtime` publication (only `alerts`, migration 0010). ConversationThread's postgres_changes INSERT subscription therefore never fires; sent/received messages appear only on a manual page reload (server component re-queries; owner passes msg_select RLS). Also latent: customers/[id]/page.tsx messages query is `order created_at ASC limit 30` → shows OLDEST 30, so a customer with >30 messages hides the newest. Fix = add messages to publication + flip to DESC-then-reverse. Not a blocker for 2A demo.
- 2026-07-25 · security · `WA_TOKEN` (Meta System User permanent token) was pasted into a session transcript during the K1 spike → **ROTATE required**: revoke in Meta Business Settings → System Users, mint a new permanent token, update Railway prod `WA_TOKEN`, redeploy. Prod DB password (shared prod+UAT) also transcript-exposed → rotate when convenient.
- 2026-07-25 · 07/PROD BUG + FIX · **Every authenticated write failed on prod with "Invalid or expired session"** (repro: owner tried Save-draft on a new quote). Root cause: prod Supabase issues **ES256 (asymmetric) access tokens** (JWKS `kid` 37c3218a…, `alg` ES256) but `api/deps.py:get_caller_uid` verified **HS256 + `SUPABASE_JWT_SECRET` only** → `jwt.decode` throws on alg mismatch → 401 before any user lookup. This blocked ALL quote/order/payment writes on prod (the JWT-forwarding path shipped in HIGH-3/HIGH-4 assumed HS256). FIX (this session, verified): `get_caller_uid` now branches on the token header `alg` — HS256 → shared secret (back-compat); ES256/RS256 → project JWKS at `${SUPABASE_URL}/auth/v1/.well-known/jwks.json` via cached `PyJWKClient`. `pyproject.toml` `pyjwt` → `pyjwt[crypto]` (pulls `cryptography`, required for ES256). New `tests/test_deps_auth.py` (5 cases: ES256 ok, HS256 back-compat, wrong-key 401, garbage 401, missing SUPABASE_URL 503). Full suite **155 passed**. Prod JWKS confirmed serving the token's kid. **DEPLOY REQUIRED**: commit → main → Railway rebuild (installs cryptography + new code); confirm `SUPABASE_URL` env set on prod API. Until deployed, dashboard writes stay broken.

- 2026-07-26 · 08 · **Migrations renumbered to 0023-0025.** Spec said 0014-0016, exec-plan said 0019-0021; both ranges were already consumed. Docs corrected in the same change (08 spec, EXECUTION_PLAN §4, PLAN.md ledger).
- 2026-07-26 · 08 · **Storage bucket policies are NOT migrations.** `scripts/pgtest.sh` applies every file in `supabase/migrations` to a bare PG15 cluster that shims only `auth` + the three roles — any `storage.*` DDL there breaks the entire empirical harness. Bucket creation + object policy live in `supabase/storage/0025_media_policies.sql`, applied by OPS per environment.
- 2026-07-26 · 08 · `current_stage` semantics settled (the 08 spec contradicted itself): NULL = never allocated; otherwise the stage the item is currently IN; on the last stage's `done`, `current_stage` STAYS `dispatch` and the new `order_items.production_done_at` is the completion marker. Initialisation belongs to the allocate API, never the trigger — a `done` on an unallocated item is a hard no-op so a stray event cannot drag an order into `in_production`.
- 2026-07-26 · 08 · **REWORK IS NOT MODELLED — client-confirmed (Darshil, this session).** A failed quality inspection does not send an item back a stage; the stage simply stays incomplete (no `done` event) and the item is `blocked` with a note until fixed. This is what licenses the partial unique index `production_events_one_done_per_stage`, which is also the hard DB backstop against a concurrent double-tap. Reversing this decision means dropping that index and adding an `attempt` column.
- 2026-07-26 · 08/db-review · Fixed before merge: **CRITICAL** a `kind='site'` photo (home interior) filed against an order escaped BOTH read boundaries, since the row policy and the bucket policy can only discriminate on `entity_type`/first path segment → CHECK `media_site_is_customer_scoped` pins `site` to `entity_type='customer'`, mirrored in `VALID_PAIRINGS`, regression-tested both sides. **HIGH** `order_items.workshop_id` had two writers and `pe_select` scopes a manager's realtime stream off it → `sync_order_item_workshop()` trigger now owns the column. **HIGH** the monotonic stage guard was a read-then-blind-write TOCTOU (the one-done-per-stage index only blocks the same stage; two different stages could race) → guard moved into the `UPDATE ... WHERE` so Postgres re-checks at lock acquisition. **HIGH** trigger fence said it may not initialise `current_stage` but the null branch did.
- 2026-07-26 · 08/db-review · **REJECTED** the reviewer's TRUNCATE-guard finding. A `before truncate` trigger on `production_events` is real protection but it also rejects `rls_support.seed_db()`'s legitimate `truncate customers … cascade` (which reaches the table via `order_items`) — it broke 33 tests. No role is granted TRUNCATE and the API never issues one, so privilege is the actual mitigation; `payments` (0016) sets the same precedent. Reasoning recorded in the migration rather than left as a silent omission.
- 2026-07-26 · JOB CARDS · **NEW SCOPE, migration 0027.** Client asked for the showroom's hand-made spec sheet (client/order/delivery date, salesperson, then per item: size, photo, product, free-text detail block) generated and sent to customers alongside the quote and to workshops as the production sheet. Built: `0027_job_cards` (`job_card_pdf` document kind; `product` + `quotation_item` media entity types; `products.primary_media_id`; `spec_notes` on both item tables), `services/job_card_html.py` (pure) + `templates/job_card.html`, `repositories/job_card_repo.py`, `tasks/job_card.py`, `api/job_cards.py` (3 routes). Photo resolution: line override → product's chosen catalog photo → product's newest ready reference → none. **The job card carries NO money — not a variant flag, there is no priced version to pick wrong** — which is what makes one file safe for both customer and outside vendor workshop, and pre-satisfies the module 13 money-blind requirement. Photos are inlined as base64 data URIs before render (Playwright must never fetch a private-bucket URL mid-render — that class of bug already cost this pipeline once, 0a43348). Tests: 19 pure + 12 empirical. **NOT in EXECUTION_PLAN §4 modules 08-13 — this is additional scope on top of the already-unpapered 2B; the Change Request must cover it before invoicing.**
- 2026-07-26 · JOB CARDS · Local verification limit: HTML render verified (money-clean, photo data-URI passes through, "No photo" fallback works); **PDF render NOT verified locally** — neither Playwright nor Jinja2 is installed in this venv (by design: the import-light rule). First real PDF must be rendered on the deployed image before this is called done.

- 2026-07-26 · 2B+job-cards/review · **code-reviewer + security-reviewer both ran and both independently found the same top defect.** Fixed: **CRITICAL/H1** `products.primary_media_id` is a bare FK to `media(id)`, and neither `job_card_repo._product_photo_key` nor the `setProductPrimaryPhoto` server action checked that the target row actually belonged to that product — so a customer's `kind='site'` photo (a home interior) could be pinned as a catalog photo, inlined into a job card, and WhatsApped to an OUTSIDE VENDOR WORKSHOP. Neither the `media_select` policy nor the bucket policy would have caught it: job-card rendering runs on the service-role connection where neither applies. Now enforced at BOTH ends (`AND m.entity_type='product' AND m.entity_id=p.id` in the read; a validating lookup before the write, since Server Actions are callable RPC and not limited to what the UI sends) and pinned by `test_foreign_media_pinned_as_primary_is_never_used_as_a_catalog_photo`. **H2** `entity_type='product'`/`'quotation_item'` uploads were unreachable for every role (0027 added the types; `media.py`/`media_repo.py` were never updated) — the catalog-photo feature was shipped non-functional. Fixed with an explicit owner/admin gate for `product` (matching products RLS) and a `quotation_items → quotations.customer_id` resolution for `quotation_item`. **H3** `MEDIA_MAX_BYTES` was unenforceable — the browser PUTs straight to a signed URL and self-reports `bytes` at `/complete`, so a 500 MB upload declared as `{"bytes": 1}` passed. Now `storage.object_size()` reads the real size from Storage, oversize is marked `failed`, and the VERIFIED size is persisted. **HIGH** workshop sends bypassed the `WA_MEDIA_ENABLED` kill switch (a manager's handset is still an outbound Cloud API document send) — now gated for both audiences. **HIGH** `_inline_photos` had no size cap; the resolver falls back to full originals when a thumbnail hasn't rendered, so a few items could push tens of MB into the worker — new `JOB_CARD_MAX_INLINE_BYTES`. **MEDIUM** send guards now run before the PDF download; `create_job_card` uses a cheap `item_count` instead of a full photo-resolving load; the `_DOC_ENTITY` identity map was deleted (two files independently assuming the same string = a latent permanent 404 on `/url` with sends still working); `/production/unallocated` is now scoped to a salesperson's assigned customers, matching the write path.
- 2026-07-26 · 2B/review · OPEN, not fixed: **M1** `spec_notes` is free text printed verbatim on a document designed for outside vendors — nothing stops staff typing a price into it, which is the one invariant the job card exists to guarantee. A `₹`/`Rs.` guard at save time is the suggested control; not built (needs a call on whether to block or warn). **M5** render/send are fire-and-forget Celery jobs returning `{"status":"queued"}` — a permanent task failure is invisible to the operator until they notice no PDF. **L1** no rate limiting: `sign-upload` mints billable Storage credentials and each job-card call queues a Playwright render on the SHARED worker pool, so a loop can starve quotation/receipt rendering — worth a per-task `rate_limit` and a separate queue. **L2** Chromium runs `--no-sandbox`.
- 2026-07-26 · 2B/review · No test exercises `tasks/job_card.py` or `tasks/media.py` at all (the branching, window/flag gates, retry paths) — same pre-existing gap as `tasks/quotes.py`/`receipts.py`, but it is where this session's real bugs lived. `job_card.html` is never rendered through Jinja2 by any test, so a template syntax error has zero coverage.

## Discoveries for later modules
<!-- things found mid-build that affect future modules -->
- 2026-07-26 · 13 · `workshop_manager` has no `order_items` SELECT policy and MUST NOT get one (`order_items` carries unit_price/line_total/gst_rate). That exclusion is exactly what forces module 13's money-blind `workshop_items` view to be a manager's only path. Note for 13: unlike `order_outstanding` (0016), that view wants the DEFAULT `security_invoker = false` — the manager has no base-table access, so the view's own WHERE is the entire boundary.
- 2026-07-26 · 09 · `my-queue` must handle a workshop with a NULL `manager_salesperson_id` (a vendor with no login), or those items become invisible. Also: `override-stage` must insert its skipped-stage `done` rows in ASCENDING sort order to stay compatible with the trigger's monotonic guard.
- 2026-07-26 · 09 · Adding an item to an order already at `ready` strands it — there is no legal `ready→in_production` edge. Enforce an API rule refusing item additions to orders not in `confirmed`.
- 2026-07-26 · 2C/REVIEW NEEDED · `0026_deliveries.sql` (deliveries + delivery_items) landed outside the 2B plan. Two problems to settle before it ships: (1) its RLS is `using (true)` / `with check (true)` for SELECT/INSERT/UPDATE to ALL authenticated — every other table in this schema scopes by `is_owner() or is_role(...) or is_assigned_to_customer(...)`, so as written a `workshop_manager` reads every delivery and any staff member can rewrite any delivery; (2) there are no `grant` statements at all, so `authenticated` currently has no table privileges and the policies are inert — the table is unreachable from the browser regardless. Also missing the `set_updated_at` trigger despite carrying `updated_at`. Phase 2C is explicitly NOT in the current SOW (EXECUTION_PLAN §5).
- 2026-07-24 · security follow-up (pre-existing, NOT 2A scope): `api/auth.py:link_salesperson` writes `salespersons.auth_uid` from a body-supplied `auth_uid` gated only by the shared DASHBOARD_API_KEY (not a verified token) — the one identity-linkage path still trusting a body field. Flagged by the JWT re-review. Tighten in a future pass. Also still open: LOW rate-limiting on public + dashboard-key routes.

## Open questions for client (Hemant) — blocks marked modules
- Product catalog / price list exists? (02; free-text lines work without it)
- Prices GST-inclusive or exclusive? HSN per product family (9401 vs 9403)? (01 — needed before GST golden tests final)
- Payment schedule policy (e.g. 50/40/10)? Receipts auto-sent to customer on WhatsApp? (05)
- Staff list + roles + phone numbers (06)
- Workshop list, managers, phones; vendor workshops get logins? (08)
- 11 production stage names EN + Gujarati confirmed (08 seed)
- Quote terms & conditions text + validity days (03)
- Turnover under ₹5cr confirmed (invoice scope) (05/06)

## External dependencies
- Meta: Phase 1 number registration + templates pending (Phase 1 chat owns this)
- 2A WhatsApp templates (module 03) — SUBMITTED 2026-07-25, all Utility/English, names match code: `payment_due` **Active**; `quote_sent`, `quote_approved_confirm` **In review** (out-of-window sends to these fail until Active; in-window text unaffected). `payment_received` dropped — no code path sends it.
- 2B templates to submit at module 08: production_started, production_completed (image), ready_for_dispatch

## Environment
- Prod Supabase project = Phase 1's (`hebnvwhuiqvbigluqfyz`). Staging/UAT project **CREATED 2026-07-25** = `TOPAZ-CRM-UAT` (`gpwdyikitstzqewvbcoi`, ap-south-1): migrations 0001-0020 applied via `supabase db push`, demo data seeded (`seed_demo.py`: 5 products, 10 customers, 6 quotes, 2 orders, payments), 3 test logins seeded (`seed_uat_logins.sql`: sales/accounts/owner @topaz.test).
- Migration head **0020 on BOTH repo, prod AND UAT** (2026-07-25). Prod was found already carrying 0010-0020 objects (applied manually via SQL editor; schema verified byte-identical to the file-built UAT) but the history table lagged at 0009 → synced with `supabase migration repair --status applied 0010..0020` (zero DDL). 0019's pipeline remap already live on prod (`design_discussion` present). Prod on FREE plan = no PITR/backup; pre-repair `pipeline_stages` snapshot kept in session scratchpad only.
- Local test DB: no `supabase start`/Docker needed — run `apps/api/scripts/pgtest.sh` (spins temp PG15 + pgvector, applies all migrations, runs pytest).
